{
  "scripts": {
    "build": "yarn run tsc",
    "start": "yarn run tsc && node dist/src/discourse-list-category.js",
    "api:build": "aspida --build"
  },
  "dependencies": {
    "@aspida/axios": "^0.5.2",
    "@types/node": "^14.0.1",
    "axios": "^0.19.2",
    "dotenv": "^8.2.0",
    "typescript": "^3.9.2"
  }
}
